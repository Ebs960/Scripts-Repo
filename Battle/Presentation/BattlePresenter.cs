using UnityEngine;

public sealed class BattlePresenter : MonoBehaviour
{
    public void Present(BattleSession session)
    {
        // Presentation wiring is intentionally separate from tactical authority.
    }
}
